# Configuración de Base de Datos
# Por defecto usa SQLite local. Para PostgreSQL usar: postgresql://user:password@localhost/dbname
DATABASE_URL=sqlite:///./approval.db

# Configuración de Correo (SMTP)
# Dejar en blanco para usar modo MOCK (imprime en consola)
# IMAP_SERVER=imap.gmail.com
# EMAIL_ACCOUNT=francojonysenati@gmail.com
# EMAIL_PASSWORD=pedro123

# Configuración de Seguridad (JWT)
# Cambiar esto en producción! Generar con: openssl rand -hex 32
SECRET_KEY=supersecretkey_change_this_in_production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Configuración CORS (Frontend URL)
# En producción, poner la URL real donde se aloja el frontend
ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
